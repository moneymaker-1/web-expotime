export const metadata = {
  title: 'Expo Time CMS',
  description: 'Expo Time Content Management System',
}

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children
}
